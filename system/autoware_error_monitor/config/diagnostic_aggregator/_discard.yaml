/**:
  ros__parameters:
    path: autoware
    discard:
      type: 'diagnostic_aggregator/AnalyzerGroup'
      path: discard
      analyzers:
        discard_all:
          type: diagnostic_aggregator/DiscardAnalyzer
          path: discard_all
          regex: [".*"]
