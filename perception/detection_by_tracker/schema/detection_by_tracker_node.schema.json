{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Parameters for Detection By Tracker Node",
  "type": "object",
  "definitions": {
    "detection_by_tracker_node": {
      "type": "object",
      "properties": {
        "ignore_unknown_tracker": {
          "type": "boolean",
          "default": true,
          "description": "If true, the node will ignore the tracker if its label is unknown."
        }
      },
      "required": ["ignore_unknown_tracker"]
    }
  },
  "properties": {
    "/**": {
      "type": "object",
      "properties": {
        "ros__parameters": {
          "$ref": "#/definitions/detection_by_tracker_node"
        }
      },
      "required": ["ros__parameters"]
    }
  },
  "required": ["/**"]
}
