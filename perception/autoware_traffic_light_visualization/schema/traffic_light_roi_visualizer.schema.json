{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Parameters for traffic_light_roi_visualizer",
  "type": "object",
  "definitions": {
    "traffic_light_roi_visualizer": {
      "type": "object",
      "properties": {
        "use_image_transport": {
          "type": "boolean",
          "description": "whether to apply image transport to compress the output debugging image in the traffic light fine detection",
          "default": "true"
        }
      },
      "required": ["use_image_transport"]
    }
  },
  "properties": {
    "/**": {
      "type": "object",
      "properties": {
        "ros__parameters": {
          "$ref": "#/definitions/traffic_light_roi_visualizer"
        }
      },
      "required": ["ros__parameters"]
    }
  },
  "required": ["/**"]
}
